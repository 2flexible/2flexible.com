```javascript
new VideoBlock(video, {...options})
```
VideoBlock class is video animation inherted from `ShapeBlock` class. VideoBlock first property must be html video element.

### autoPlay
Video animation will play automatically when added to canvas.

```javascript
block.autoPlay(option)
```

**Option**
* **type** : boolean
* **default**: false

**Returns**
* **autoPlay** : boolean